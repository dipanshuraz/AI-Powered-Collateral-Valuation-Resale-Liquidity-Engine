# Optional: GPT-style summary of the server-computed JSON (numbers are not invented by the model).
OPENAI_API_KEY=
# Optional override (default gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Optional: browser Cookie header for listing JSON APIs (demo; respect each site’s ToS).
# MAGICBRICKS_COOKIE=
# ACRES99_COOKIE=
# HOUSING_COOKIE=
# NOBROKER_COOKIE=
# Shared fallback if you use one cookie for multiple portals:
# LISTING_PORTALS_COOKIE=
